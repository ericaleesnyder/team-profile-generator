function generateCard (employee) {
  // will be passing one employee object per office hours demo
}

function generateHtml (compiledTeam) {
  console.log("(generating HTML)")
}

module.exports = generateHtml